name: Weekly Keyword Research

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Feeder keyword research is disabled. The main Tow With The Flow repository owns keyword strategy."
